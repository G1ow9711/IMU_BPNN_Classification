# 工程代码生成规范

本文件适用于当前工程及其全部子目录，包括 Python 训练端、ESP32 嵌入式端和上位机端。任何新增、修改或重构的代码都必须遵守以下规则。

## 1. 中文逐行注释

1. 生成任何代码时，必须添加详细、准确的中文逐行注释。
2. 每条具有实际含义的代码语句都必须说明其用途、输入、输出或状态变化，不能只在函数开头写一段概括性注释。
3. 变量声明必须说明变量表示的物理量或业务含义；涉及传感器数据时还要写明单位、坐标系和有效范围。
4. 条件分支必须说明判断依据以及进入该分支的业务或算法含义。
5. 循环必须说明遍历对象、终止条件、每轮处理内容和结果累积方式。
6. 函数调用必须说明调用目的；参数含义不直观时，应在调用处补充说明。
7. 返回语句必须说明返回值含义；存在错误码时，应说明各错误码对应的原因。
8. 不能使用“初始化变量”“进行计算”“处理数据”等没有具体信息的空泛注释。
9. 注释必须与代码同步更新，禁止保留失效、矛盾或复制粘贴错误的注释。
10. 对括号、纯格式行、显而易见的命名空间结束行，可不单独添加注释；其所属代码块的目的必须在相邻位置说明清楚。

## 2. Python 训练端

1. 数据读取、单位换算、窗口切分、数据过滤、数据增强、特征提取、标准化、训练、验证、测试和模型导出代码均需逐行中文注释。
2. NumPy 或 PyTorch 张量必须注明形状约定，例如 `[样本数, 时间点数, 六轴通道数]`。
3. 六轴 IMU 通道顺序必须明确注明，例如 `gx、gy、gz、ax、ay、az`，不能依赖读者猜测。
4. 每个 epoch 的损失、验证准确率、宏平均 F1、逐类召回率和最弱类别指标必须保留可见日志说明。
5. 模型结构、隐藏层维度、激活函数、损失函数、优化器和早停规则必须在代码附近写明选择理由。
6. 模型参数、标准化参数、类别顺序和特征顺序导出时，必须说明它们与 ESP32 推理端的对应关系。

## 3. ESP32 端

1. C/C++ 源文件、头文件、Arduino/ESP-IDF 代码和自动生成的模型头文件均需详细中文注释。
2. IMU 初始化、采样频率、量程、单位换算、环形缓冲区、窗口触发、特征计算、BP 前向传播和分类输出均需逐行说明。
3. 数组必须注明维度、元素顺序和内存占用；指针必须说明指向对象、有效生命周期和是否允许为空。
4. 浮点运算必须说明输入单位及数值范围；可能发生除零、溢出、下溢或精度损失的位置必须写明保护方法。
5. Python 与 ESP32 的特征名称、特征顺序、标准化公式、网络层顺序和激活函数必须完全一致，并在对应代码处注明一致性约束。
6. 自动生成的 C 头文件也必须生成中文说明，至少覆盖常量、数组维度、模型层、特征提取函数和推理函数。

## 4. 上位机端

1. 串口/BLE/网络通信、数据帧解析、校验、缓存、线程、界面更新、日志和异常处理均需逐行中文注释。
2. 通信协议必须说明帧头、长度、命令字、数据区、校验字段、字节序和超时策略。
3. 后台线程与 UI 线程之间的数据传递必须说明线程安全措施，禁止在注释不明的情况下直接跨线程更新界面。
4. 图表坐标轴、单位、刷新频率、数据窗口长度和动作类别显示逻辑必须在实现处说明。
5. 用户可见错误必须说明触发条件、恢复方式和是否影响后续数据。

## 5. 算法与公式说明

凡涉及算法计算、信号处理、特征提取、统计指标、损失函数、标准化、滤波或模型推理，除逐行中文注释外，还必须提供详细的公式原理介绍。

公式说明至少包含：

1. 算法目的及适用场景。
2. 完整数学公式，使用 Markdown/LaTeX 表达。
3. 公式中每个符号、下标和常量的定义。
4. 输入、输出、张量形状、物理单位和取值范围。
5. 从原始数据到计算结果的步骤说明。
6. 数值稳定措施、边界条件和异常输入处理。
7. 时间复杂度、空间复杂度以及 ESP32 上的资源影响。
8. 该特征为什么有助于区分对应健身动作的物理解释。
9. Python 实现与 ESP32 C/C++ 实现之间的一致性要求和误差容限。

标准化必须说明：

$$
z_i = \frac{x_i - \mu_i}{\sigma_i}
$$

其中，$x_i$ 是第 $i$ 个原始特征，$\mu_i$ 是训练集上该特征的均值，$\sigma_i$ 是训练集上该特征的标准差，$z_i$ 是送入 BP 网络的标准化结果。当 $\sigma_i$ 小于设定阈值时，必须说明用于避免除零的处理方式。

BP 全连接层必须说明：

$$
\mathbf{y} = f\left(\mathbf{W}\mathbf{x} + \mathbf{b}\right)
$$

其中，$\mathbf{x}$ 是输入向量，$\mathbf{W}$ 是权重矩阵，$\mathbf{b}$ 是偏置向量，$f(\cdot)$ 是激活函数，$\mathbf{y}$ 是该层输出。实现中必须注明矩阵维度、存储顺序和激活函数的具体形式。

## 6. 文档位置

1. 简短公式和局部算法说明应直接写在代码注释或函数文档字符串中。
2. 较长推导应保存到 `docs/` 下的中文 Markdown 文档，并在代码注释中给出对应文档路径和章节名称。
3. README 必须用中文说明 Python 端、ESP32 端和上位机端的运行方法、数据流、主要算法及生成物。
4. 修改算法时，必须同时更新代码注释、公式文档、README 和相关测试说明。

## 7. 交付检查

提交代码前必须检查：

1. 新增或修改的有效代码行是否均有对应的详细中文解释。
2. 算法代码是否包含公式、符号定义、单位、边界条件和复杂度说明。
3. Python 与 ESP32 的特征和 BP 推理是否保持顺序及数值一致。
4. 上位机通信协议、线程和界面更新逻辑是否说明完整。
5. 注释是否准确反映当前实现，而不是描述旧版本行为。
6. 测试是否覆盖正常输入、边界输入、异常输入和 Python/C 一致性。

不满足以上要求的代码视为未完成，不得作为最终交付版本。

## 8. Waveshare 手表官方程序基线

本工程目标硬件为 Waveshare `ESP32-S3-Touch-AMOLED-2.06`。涉及屏幕、触摸、PMIC、IMU、RTC、音频、Flash 分区、启动时序或低功耗的改动，必须先参考厂家第一方仓库：

- 官方仓库：<https://github.com/waveshareteam/ESP32-S3-Touch-AMOLED-2.06>
- ESP-IDF 例程根目录：<https://github.com/waveshareteam/ESP32-S3-Touch-AMOLED-2.06/tree/main/examples/esp-idf>
- Arduino 例程根目录：<https://github.com/waveshareteam/ESP32-S3-Touch-AMOLED-2.06/tree/main/examples/arduino>
- 出厂与恢复固件：<https://github.com/waveshareteam/ESP32-S3-Touch-AMOLED-2.06/tree/main/FirmWare>
- 硬件原理图：<https://github.com/waveshareteam/ESP32-S3-Touch-AMOLED-2.06/tree/main/Schematic>

### 8.1 官方例程用途映射

1. 显示和触摸必须优先对照 `examples/esp-idf/02_lvgl_demo_v9`。其最小入口顺序是 `bsp_display_start()`、`bsp_display_lock(0)`、创建或更新 LVGL 对象、`bsp_display_unlock()`。
2. AXP2101 供电、电池电压、电量和充电状态必须优先对照 `examples/esp-idf/01_AXP2101` 及 `examples/arduino/05_LVGL_AXP2101_ADC_Data`。
3. QMI8658 六轴 IMU 初始化、量程和实时数据显示必须优先对照 `examples/arduino/04_LVGL_QMI8658_ui`；项目算法仍固定使用 `gx、gy、gz、ax、ay、az` 顺序和训练端物理单位。
4. PCF85063 RTC 必须优先对照 `examples/arduino/03_LVGL_PCF85063_simpleTime`。
5. LVGL 9 的 Arduino 接线可参考 `examples/arduino/06_LVGL_Arduino_v9`，但 ESP-IDF 产品固件不得直接混入 Arduino 显示初始化代码。
6. 字符显示最小隔离可参考 `examples/arduino/02_GFX_AsciiTable`；该例程只能用于区分面板/字库问题，不能替代最终 LVGL 中文界面验收。

### 8.2 硬件版本与依赖锁定

1. 厂家仓库不同时间点可能对应不同面板或触摸控制器。当前仓库 README 标注 CO5300/CST9220，而部分历史 `02_lvgl_demo_v9/dependencies.lock` 使用 SH8601/FT5x06。禁止把两套驱动、初始化命令或触摸坐标配置混合使用。
2. 开发前必须记录真板版本、原理图版本、厂家例程 Git 提交、BSP 版本、`dependencies.lock`、ESP-IDF 版本和可成功烧录的镜像 SHA-256。
3. 真板控制器身份不明确时，先完整烧录对应厂家例程做 A/B；以“同一块板稳定显示、触摸正常、COM 端口不重枚举”的官方工件为基线，再确定驱动族。
4. 不得仅因组件目录名、旧缓存或 README 描述就认定控制器型号；三个证据冲突时停止自定义驱动修改，先核对原理图、BSP 源码和稳定官方镜像的清单。
5. 官方示例、BSP 和受管组件必须来自同一版本链。禁止锁文件显示新版本、源码仍为旧版本，或手工复制半套 BSP。

### 8.3 显示与 LVGL 强制规则

1. 未取得真板 A/B 证据前，保留官方显示初始化、面板 IO、颜色格式、字节交换、旋转、缓冲位置、缓冲行数、PSRAM/DMA 选择和 flush 完成回调。
2. 所有 `lv_` 对象创建、属性更新和页面切换必须位于官方 BSP 的 LVGL 锁内；后台任务只能通过队列把数据交给 UI 任务。
3. 厂家 QSPI flush 为异步链时，禁止在持有 BSP LVGL 锁的更新函数内调用 `lv_refr_now()`；该组合已在真板造成反复黑/白屏和看门狗式重启。
4. 不得同时修改面板驱动、LVGL 布局、字体、亮度和低功耗。每轮只改变一个变量，并保留上一版可完整恢复的四段镜像。
5. 显示验收必须包含：连续 30 秒文字边缘无重影、圆角安全区无遮挡、触摸坐标正确、COM 端口不掉线；仅“屏幕点亮”不算通过。
6. 官方例程能稳定而自定义固件异常时，默认判定为自定义集成问题，不得归因于硬件损坏。

### 8.4 低功耗与烧录规则

1. UI、IMU、BLE 和业务链未稳定前，禁止启用自动熄屏、Light-sleep、Deep-sleep 或 PMIC 关屏动作。
2. 低功耗必须在官方 AXP2101 基线上逐级加入：仅降亮度、关闭面板、Light-sleep、最后才是 Deep-sleep；每级单独验证 POWER/BOT 唤醒和 USB 稳定性。
3. 烧录必须使用 ESP-IDF 对应工具链并显示完整过程；写入地址、镜像长度、SHA-256 和每段 Hash 校验必须记录。
4. COM 端口不稳定时只守候精确 `VID_303A&PID_1001` 设备，不操作其它串口或共享 USB 根集线器。
5. 每次自定义固件烧录前必须确认可回退到厂家恢复固件或已验证稳定的官方 LVGL Demo。

### 8.5 官方基线偏离说明

任何偏离官方例程的实现，都必须在 `findings.md` 或对应 `docs/` 文档中写明：

1. 官方原实现和精确来源路径。
2. 偏离原因及要解决的单一问题。
3. 预期风险，尤其是面板 flush、内存、任务栈、电源和 USB 枚举风险。
4. 源码测试、构建日志、烧录日志和真板 A/B 结果。
5. 失败后的恢复镜像及回退步骤。

## 9. Git 管理范围

1. 本仓库唯一允许的开发、Git、构建和运行根目录是 `G:\Free_Project\BiShengBei_BPNN_ESP32\Project`；后续任务必须先确认当前目录位于该路径内。
2. 禁止为本仓库创建或使用 linked worktree，也不得在 `G:\Free_Project\BiShengBei_BPNN_ESP32\worktrees` 或其它目录维护第二份工程副本。需要分支开发时，必须直接在 `Project` 内切换分支。
3. 本仓库日常 Git 管理只覆盖三个产品工程：Python 训练与模型工程 `python/`、ESP32 工程 `esp32/`、上位机工程 `pc/`。
4. 执行 `git add`、提交、变基、推送或创建 PR 时，默认只允许包含 `python/**`、`esp32/**` 和 `pc/**`；不得把根目录训练数据、`docs/`、`tools/`、`shared/`、`artifacts/`、`animation-plans/`、分析结果、构建目录、缓存或本地运行产物带入产品提交。
5. 根目录 `Agents.md` 是范围规则本身的唯一常规例外；只有修改工程协作规则时才允许与三个产品工程一起提交。
6. 三个产品工程之外的文件即使已经被 Git 跟踪或处于修改状态，也必须保留在工作树中且不得暂存；确需提交时必须先取得用户明确授权。
7. 提交前必须用限定路径的暂存清单复核范围，并确认 `git diff --cached --name-only` 中只有 `Agents.md`、`python/`、`esp32/` 或 `pc/`。
