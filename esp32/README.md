# ESP32-S3 端

本目录保存 ESP32-S3 推理、动作状态机和设备通信代码。

`include/esp32_bp_model.h` 由 Python 训练端生成，包含：

- 264 项手工特征提取；
- 标准化参数；
- BP 网络权重和偏置；
- 动作类别名称；
- 运动强度与活动比例辅助函数；
- 窗口到类别的完整推理函数。

导出器支持可选的 BP 专家网络和特征索引映射，但当前正式路线使用单个平铺 BP。未达到逐类门槛前，本目录只保留接口位置，不提交诊断模型。

只有独立测试集 11 类动作的召回率全部达到 90% 后，正式模型头文件才会进入本目录。
